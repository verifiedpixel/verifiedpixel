'use strict';

describe('imagelist service', function() {
    'use strict';

    // TODO: fix this, fails whenever included
    //beforeEach(module('verifiedpixel.imagelist'));

    it('can include the verifiedpixel.imagelist module', inject(function() {
        expect(true).toBe(true);
    }));

});
